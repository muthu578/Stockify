import { describe, it, expect, vi } from 'vitest';
// import purchaseController from '../purchaseController';

describe('purchaseController Controller', () => {
    it('should handle request correctly', () => {
        expect(true).toBe(true);
    });
});
