import { describe, it, expect, vi } from 'vitest';
// import hrController from '../hrController';

describe('hrController Controller', () => {
    it('should handle request correctly', () => {
        expect(true).toBe(true);
    });
});
