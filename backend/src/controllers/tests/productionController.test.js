import { describe, it, expect, vi } from 'vitest';
// import productionController from '../productionController';

describe('productionController Controller', () => {
    it('should handle request correctly', () => {
        expect(true).toBe(true);
    });
});
