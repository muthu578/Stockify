import { describe, it, expect, vi } from 'vitest';
// import contactController from '../contactController';

describe('contactController Controller', () => {
    it('should handle request correctly', () => {
        expect(true).toBe(true);
    });
});
