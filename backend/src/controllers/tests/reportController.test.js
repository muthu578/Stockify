import { describe, it, expect, vi } from 'vitest';
// import reportController from '../reportController';

describe('reportController Controller', () => {
    it('should handle request correctly', () => {
        expect(true).toBe(true);
    });
});
