import { describe, it, expect, vi } from 'vitest';
// import machineController from '../machineController';

describe('machineController Controller', () => {
    it('should handle request correctly', () => {
        expect(true).toBe(true);
    });
});
