import { describe, it, expect, vi } from 'vitest';
// import financeController from '../financeController';

describe('financeController Controller', () => {
    it('should handle request correctly', () => {
        expect(true).toBe(true);
    });
});
