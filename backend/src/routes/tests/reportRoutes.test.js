import { describe, it, expect, vi } from 'vitest';
// import express from 'express';
// import reportRoutes from '../reportRoutes';

describe('reportRoutes Routes', () => {
    it('should define the routes correctly', () => {
        expect(true).toBe(true);
    });
});
