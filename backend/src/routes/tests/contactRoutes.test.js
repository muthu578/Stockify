import { describe, it, expect, vi } from 'vitest';
// import express from 'express';
// import contactRoutes from '../contactRoutes';

describe('contactRoutes Routes', () => {
    it('should define the routes correctly', () => {
        expect(true).toBe(true);
    });
});
