import { describe, it, expect, vi } from 'vitest';
// import express from 'express';
// import hrRoutes from '../hrRoutes';

describe('hrRoutes Routes', () => {
    it('should define the routes correctly', () => {
        expect(true).toBe(true);
    });
});
