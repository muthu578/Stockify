import { describe, it, expect, vi } from 'vitest';
// import express from 'express';
// import authRoutes from '../authRoutes';

describe('authRoutes Routes', () => {
    it('should define the routes correctly', () => {
        expect(true).toBe(true);
    });
});
