import { describe, it, expect, vi } from 'vitest';
// import express from 'express';
// import financeRoutes from '../financeRoutes';

describe('financeRoutes Routes', () => {
    it('should define the routes correctly', () => {
        expect(true).toBe(true);
    });
});
