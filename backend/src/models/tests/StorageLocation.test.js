import { describe, it, expect, vi } from 'vitest';
// import StorageLocation from '../StorageLocation';

describe('StorageLocation Model', () => {
    it('should define the schema correctly', () => {
        expect(true).toBe(true);
    });
});
